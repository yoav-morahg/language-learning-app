package com.yoavmorahg.learner_app.entity;

import java.io.Serializable;

//@Embeddable
public class ItemCollectionKey implements Serializable {
/*
    @Column(name = "collection_id")
    Long collectionId;

    @Column(name = "item_id")
    Long itemId;

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        ItemCollectionKey that = (ItemCollectionKey) o;
        return Objects.equals(collectionId, that.collectionId) && Objects.equals(itemId, that.itemId);
    }

    @Override
    public int hashCode() {
        return Objects.hash(collectionId, itemId);
    }


 */
}
